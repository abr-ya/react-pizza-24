import { Button } from "../../components";

const Demo = () => (
  <>
    <h1>Hello, Vite + React!)</h1>
    <Button onClick={() => console.log("click!")}>Click me!)</Button>
  </>
);

export default Demo;
